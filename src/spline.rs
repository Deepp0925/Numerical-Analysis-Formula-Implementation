/// returns the natural cubic spline of given data points
/// # Arguments
/// * `x_points` - the x values of the data points
/// * `y_points` - the y values of the data points
/// # Return
/// `String` - the string representation of the spline

fn process_points(points: Vec<Vec<f64>>) -> Vec<Vec<f64>> {}
